import MirrorMembrane from "#mirror_membranes/source/MirrorMembrane.js";

describe("MirrorMembrane", () => {
  xit("is not ready to go", () => {
    void(MirrorMembrane);
    expect(true).toBe(false);
  });
});
