name: test-env
on:
  push:
    paths:
      - '.github/workflows/tsm.test.yml'
jobs:
  build-stage-one:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: './projects/ts-morph-structures'
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - run: echo $PWD
